{
  "name": "your-energy",
  "version": "1.0.0",
  "private": true,
  "description": "Your Energy — fitness exercises SPA",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
