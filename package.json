{
  "name": "your-energy",
  "version": "1.0.0",
  "private": true,
  "description": "Your Energy — fitness exercises SPA",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
